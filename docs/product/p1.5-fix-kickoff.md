<!--
SPDX-License-Identifier: Apache-2.0
Copyright 2026 The Hearth Authors
-->
# Phase 1.5 — Security Fix Pass (all flagged items) — Claude Code kickoff prompt

> Owner decision (2026-06-03): fix **all nine flagged items** from the Phase 1.5 review, aiming close to
> public-1.0-ready before any beta. This is a focused **security-hardening fix pass** — not new features. Run
> it in a fresh Code session. Every fix gets a **regression test that asserts the hole is closed.** Authoritative
> finding detail is in **[docs/SECURITY-REVIEW.md](../SECURITY-REVIEW.md)** (items F-A … F-M3).
> The real-host validation ([docs/REAL-HOST-VALIDATION.md](../REAL-HOST-VALIDATION.md)) runs in parallel as a
> human task — independent of this pass.

---

```
Phase 1.5 — Security Fix Pass. The owner reviewed the Phase 1.5 findings and chose to fix ALL nine flagged
items (F-A..F-M3) + guard tenant_id, toward public-1.0-readiness. Hardening only — no new product features.

STEP 0 — IDEMPOTENCY GUARD: read PROJECT-STATE.md + `git log --oneline`. If these fixes are already committed,
STOP and report. Confirm the P1.5 baseline is green in Docker (Pest 289 / Dusk 2) and the tree is clean.

START: read PROJECT-STATE.md, CLAUDE.md, docs/SECURITY-REVIEW.md (the F-x findings + recommended fixes), and
the relevant specs (security-and-permissions §2 for anti-spam, §1 for permissions, §4 for OWASP).

MODEL/EFFORT: Opus 4.8 at xhigh — every item is security-sensitive. RULES OF ENGAGEMENT: each fix lands with a
regression test that proves the hole is closed; keep approved behavior intact (the spec's "flag-don't-block on
uncertainty" still holds — don't over-rotate anti-spam into false-positive hard-blocks); if any fix becomes a
real design change, STOP and flag it. Small conventional DCO commits. Open with a short plan, then proceed.

GROUP 1 — ANTI-SPAM HARDENING (the differentiator):
  • F-B registration anti-abuse: a per-IP rate limit on POST /register (429 on abuse); make honeypot+timing
    MANDATORY (reject when the timing token is absent OR too-fast — close the skip); bind each Q&A challenge to
    a single-use server-side nonce so a captured answer can't be replayed. Keep the test-env opt-out (captcha/
    SFS off) working so M1's RegistrationTest stays green. Tests: throttle 429, missing/fast timing rejected,
    replayed nonce rejected.
  • F-C StopForumSpam fail-open: add a cron WARM job that refreshes the cron-cached blocklist (never cold);
    fix the degradation so a cold-cache + API-unreachable high-confidence/unknown registrant is FLAGGED to the
    moderation queue (status=pending), NOT allowed (closes fail-open) and NOT hard-blocked (honors flag-don't-
    block). Tests: API-down + cold cache → pending (not allowed); warm job populates the cache.
  • F-D trust-promotion gaming: replace self-post-count-only promotion with the spec's TL0→TL1 signals
    (security §2.3): topics-read (use M4's topic_reads) + time-since-registration + ≥1 post + NO active flags;
    keep configurable. The TL0 link/image NEVER gate must not be liftable by patient self-posting alone. Tests:
    a self-poster without read/time signals stays TL0 (links suppressed); legitimate activity promotes.
  • F-E suppression-on-demotion: on a trust demotion (or gate-lowering flag), invalidate the affected user's
    posts' body_html_cache so link/image suppression re-applies on next render (event-driven). Test: demote →
    previously-rendered links are suppressed on re-render.

GROUP 2 — AUTHZ / MODERATION INTEGRITY:
  • F-F actor-vs-target rank check: a staff member cannot ban/warn/spam-clean a target of equal-or-higher rank
    (compare max group priority / admin status); admins can action anyone, mods cannot action admins (or each
    other — configurable). Tests: mod→admin ban/warn = 403; mod→member ok; admin→mod ok.
  • F-H guard tenant_id: remove `tenant_id` from User::$fillable (it's the last dangerous field after M-1). Test:
    mass-assigning tenant_id is ignored.
  • F-G ACL-model mass-assignment: add explicit safe $fillable/$guarded to AclEntry, Role, RolePermission,
    Group, Permission, role_assignments — taking care the seeders/RoleExpander still write needed fields (use
    forceFill where they relied on mass-assignment). Test: a crafted protected-field mass-assign is ignored; the
    seed suite stays green.

GROUP 3 — INSTALL WINDOW + FORENSICS:
  • F-A install setup token: the pre-install boot writes a random token to storage/install-token.txt (0600);
    the wizard (step 1) and `hearth:install` require it — wrong/absent token → refuse. This makes the
    unauthenticated window safe (needs filesystem read) and gates the SSRF primitive. Consume/delete the token
    on successful install. Update docs/REAL-HOST-VALIDATION.md (retrieve the token via FTP/cPanel file manager).
    Test: installer refuses without the token, accepts with it, token consumed on install.
  • F-I auth audit logging: listen to the Fortify/auth events (login, failed login, logout, password reset, 2FA
    enable/disable/challenge/recovery) → structured audit_log rows (actor, event, ip, ua). Test: login/failed/
    logout each write an audit_log entry.

GROUP 4 — STRICT CSP:
  • F-M3 strict nonce-CSP: emit a per-request nonce; apply it to Livewire's injected scripts (nonce config) +
    any inline scripts/styles; adopt Alpine's CSP-compatible build; refactor inline style="…" to classes/nonce.
    JUDGMENT CALL: if the inline-style refactor is large/invasive, ship the strict nonce-CSP BEHIND A CONFIG
    TOGGLE (default stays the safe baseline CSP already shipped; strict opt-in once the app is nonce-clean) and
    REPORT exactly what remains to make it default-on. Do NOT ship a half-applied strict CSP that breaks the
    editor/Livewire/Alpine. Whatever ships, the Dusk editor journey MUST stay green.

DEFINITION OF DONE: every item F-A..F-M3 + tenant_id is Fixed-with-test, OR (F-M3 only, if invasive) shipped
behind a toggle with a clear report of what's left. Update docs/SECURITY-REVIEW.md (each F-x → Fixed / Deferred-
with-reason). Full M0–M5 + P1.5 suites STAY green; Dusk green under the shipped CSP; Pint, Larastan, composer
audit, npm audit all clean. Runs on the baseline tier. Report back: per-item status + anything newly flagged.

SCOPE FENCE: hardening only — nothing from Phase 2/3/4. Don't weaken "flag-don't-block on uncertainty." Don't
break approved behavior; flag any genuine design change for owner decision.
```

---

## When this reports back

The Cowork session reviews each fix against its finding (does the regression test actually prove the hole is
closed?), confirms the anti-spam changes didn't introduce false-positive hard-blocks, and checks F-M3's posture.
Combined with **your real-host validation results**, that closes the pre-beta gate — at which point Hearth is
ready for a beta, with only the independent third-party security review + the WCAG/load/i18n work remaining
before a public 1.0.
